spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #nacos注册中心地址
    sentinel:
      transport:
        dashboard: localhost:8080 #sentinel控制台地址
    #  eager: true #是否延迟加载 false：是 true：否